{
  "name": "calculator",
  "version": "1.0.0",
  "description": "Calculator",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "parcel:rmcache": "rm -rf .parcel-cache dist",
    "parcel:compile": "parcel index.html",
    "parcel:watch": "nodemon --watch src --watch index.html --ext js,scss,html --exec \"parcel index.html\"",
    "dev": "npm-run-all parcel:rmcache parcel:watch",
    "parcel:build": "parcel build index.html"
  },
  "author": "Marc Diallo",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.15.2",
    "npm-run-all": "^4.1.5",
    "parcel": "^2.15.2",
    "sass": "^1.89.2"
  },
  "dependencies": {
    "core-js": "^3.43.0",
    "regenerator-runtime": "^0.14.1"
  }
}
